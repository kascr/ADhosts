<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 查看网络连接 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 运行前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 此应用可显示在其他应用上方 -->
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" /> <!-- 允许应用获取存储 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.function.Metal"
            android:exported="false" /><!-- 金属检测 -->
        <activity
            android:name=".ui.activity.function.Decibel"
            android:exported="false" /><!-- 声音检测 -->
        <activity
            android:name=".ui.activity.StartActivity"
            android:exported="true"><!-- 启动页 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoAnimation"
            android:windowSoftInputMode="adjustResize" />

        <!-- 订阅刷新广播接收器 -->
        <receiver
            android:name=".ui.receiver.SubscriptionUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kascr.adhosts.SUBSCRIPTION_UPDATED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>